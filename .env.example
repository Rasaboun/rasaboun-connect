# navitia-proxy Worker URL (see ../navitia-proxy). The only config the bundle
# needs: the Worker holds the IDFM/Navitia apiKey AND serves the MapKit JS token
# (/mapkit-token), so no tokens ship here. Empty → demo data + no map.
VITE_NAVITIA_PROXY_URL=
